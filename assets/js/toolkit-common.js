/* Saraaf Trade Toolkit - shared tool controls */
(function () {
  const $ = id => document.getElementById(id);
  const DATA_ENDPOINT = window.SARAAF_DATA_ENDPOINT || 'https://script.google.com/macros/s/AKfycbxGQqfnu1wA00-1Y0Nqeet6189pS2gO14VSY1yd4jRQmgBz9xRxjjyS7ELv10QTeEkQvA/exec';
  function toolId(){return document.body.dataset.toolId||location.pathname.split('/').filter(Boolean).pop()||'unknown';}
  function suiteId(){const parts=location.pathname.split('/').filter(Boolean);const i=parts.indexOf('toolkit');if(i>=0&&parts[i+1]&&parts[i+2])return parts[i+1];return document.body.dataset.suite||'global';}
  function fieldSummary(){const rows=[];document.querySelectorAll('input,select,textarea').forEach(el=>{if(el.closest('.tool-feedback')||!el.id||['button','submit'].includes(el.type))return;const label=document.querySelector('label[for="'+el.id+'"]');const name=label?label.textContent.trim():el.id;if(el.value!=='')rows.push(name+': '+el.value);});return rows.join('\n');}
  function resultSummary(){const results=[];document.querySelectorAll('.result-value,[data-share-result],#total-value,#ledger-total,#output-total-value,#output-total,#output-finance-cost,#output-target-price,#output-moisture-weight,#output-cbm-total').forEach(el=>{const text=el.textContent.trim();if(text&&!results.includes(text))results.push(text);});return results.length?'\n\nResults:\n'+results.join('\n'):'';}
  function makeShareText(){const title=document.querySelector('[data-tool-title]')?.textContent.trim()||document.title;return 'Saraaf Trade Toolkit — '+title+'\n\n'+fieldSummary()+resultSummary()+'\n\nGenerated: '+new Date().toLocaleString();}
  function logUsage(eventType,extra={}){if(!DATA_ENDPOINT)return;const payload={eventType,suite:suiteId(),toolId:toolId(),toolTitle:document.querySelector('[data-tool-title]')?.textContent.trim()||document.title,fields:fieldSummary(),results:resultSummary(),at:new Date().toISOString(),...extra};try{fetch(DATA_ENDPOINT,{method:'POST',mode:'no-cors',headers:{'Content-Type':'text/plain;charset=utf-8'},body:JSON.stringify(payload),keepalive:true}).catch(()=>{});}catch(e){}}
  async function shareQuote(){const text=makeShareText();logUsage('share',{quoteText:text});if(navigator.share){try{await navigator.share({title:document.title,text});return;}catch(e){if(e.name==='AbortError')return;}}try{await navigator.clipboard.writeText(text);const btn=$('shareBtn');if(btn){btn.textContent='Quote Copied!';setTimeout(()=>btn.textContent='Share Quote',1600);}}catch(e){window.prompt('Copy this quotation:',text);}}
  function addFeedback(){if($('tool-feedback'))return;const box=document.createElement('section');box.id='tool-feedback';box.className='card tool-feedback';box.innerHTML='<h2>Feedback</h2><p class="feedback-note">Rate this tool and tell us what would make it more useful for your trade workflow.</p><div class="feedback-row"><div class="tool-stars" aria-label="Rate this tool">'+[1,2,3,4,5].map(n=>'<button class="tool-star" type="button" data-star="'+n+'" aria-label="'+n+' stars">★</button>').join('')+'</div><span id="feedback-rating" class="feedback-note"></span></div><textarea id="tool-feedback-text" placeholder="Optional comment"></textarea><button id="feedback-submit" class="btn-primary feedback-submit" type="button">Submit Feedback</button><p id="feedback-status" class="feedback-note"></p>';const host=document.querySelector('.tool-main')||document.querySelector('.max-w-7xl');if(!host)return;host.appendChild(box);box.querySelectorAll('.tool-star').forEach(btn=>btn.addEventListener('click',()=>{const stars=Number(btn.dataset.star);box.querySelectorAll('.tool-star').forEach((s,i)=>s.style.color=i<stars?'#0480e4':'#627885');$('feedback-rating').textContent=stars+' / 5';box.dataset.rating=stars;}));$('feedback-submit')?.addEventListener('click',()=>{const comment=$('tool-feedback-text')?.value.trim();const rating=box.dataset.rating||'';if(!rating&&!comment){$('feedback-status').textContent='Please add a rating or comment before submitting.';return;}logUsage('feedback',{rating,comment});$('feedback-status').textContent='Thank you for your feedback.';$('tool-feedback-text').value='';});}
  function addPrintModeButton(){const actions=document.querySelector('.tool-actions');if(!actions||document.getElementById('bwPrintBtn'))return;const btn=document.createElement('button');btn.id='bwPrintBtn';btn.className='tool-action';btn.type='button';btn.textContent='B&W PDF';btn.addEventListener('click',()=>{document.body.classList.add('print-bw');logUsage('print_bw_requested',{quoteText:makeShareText()});window.print();});window.addEventListener('afterprint',()=>document.body.classList.remove('print-bw'));actions.appendChild(btn);}
  function init(){$('printBtn')?.addEventListener('click',()=>{document.body.classList.remove('print-bw');logUsage('print_requested',{quoteText:makeShareText()});window.print();});$('shareBtn')?.addEventListener('click',shareQuote);addFeedback();addPrintModeButton();if('serviceWorker'in navigator)navigator.serviceWorker.register('../sw.js').catch(()=>{});}
  window.SaraafToolkit={shareQuote,makeShareText,logUsage,suiteId};document.addEventListener('DOMContentLoaded',init);
})();
